{
	"name": "chat-app-golang-backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"server:build": "go build -o cmd cmd/main.go",
		"server:dev": "nodemon --watch ./**/*.go --exec go run cmd/main.go"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^3.0.1"
	}
}
